{
  "name": "umat-pugeran-dalam-angka",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Web data story profil umat Paroki HKTY Pugeran — satu halaman naratif, statis, dapat dibuka tanpa jaringan.",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "data": "node scripts/run_pipeline.mjs 00_load.py 01_clean.py 02_aggregate.py",
    "validate:data": "node scripts/run_pipeline.mjs 03_validate.py",
    "validate:palette": "node scripts/validate_palette.mjs",
    "build": "astro check && astro build && node scripts/build_sw.mjs",
    "verify": "npm run data && npm run validate:data && npm run validate:palette && npm run build && npm run check:build",
    "check:build": "node scripts/check_build.mjs"
  },
  "dependencies": {
    "astro": "^7.3.2"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.5",
    "typescript": "^5.9.2"
  },
  "allowScripts": {
    "esbuild@0.28.2": true
  }
}
